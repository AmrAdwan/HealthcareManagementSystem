@model HealthcareManagementSystem.Models.MedicalRecord

@{
  ViewData["Title"] = "Delete Medical Record";
}

<h1>Delete Medical Record</h1>

<h4>Medical Record</h4>
<hr />
<div class="row">
  <div class="col-md-10">
    <h4>Are you sure you want to delete this medical record?</h4>
    <div>
      <h5>Medical Record</h5>
      <div>
        <dl class="row">
          <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.Date)
          </dt>
          <dd class="col-sm-10">
            @Html.DisplayFor(model => model.Date)
          </dd>
          <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.Description)
          </dt>
          <dd class="col-sm-10">
            @Html.DisplayFor(model => model.Description)
          </dd>
        </dl>
      </div>
      <form asp-action="DeleteMedicalRecord">
        <input type="hidden" asp-for="MedicalRecordId" />
        <input type="hidden" asp-for="PatientId" />
        <div class="form-group">
          <input type="submit" value="Delete" class="btn btn-danger" /> |
          <a asp-action="MedicalRecords" asp-route-id="@Model.PatientId" class="btn btn-secondary">Cancel</a>
        </div>
      </form>
    </div>
  </div>
</div>
@section Scripts {
  @{
    await Html.RenderPartialAsync("_ValidationScriptsPartial");
  }
}
